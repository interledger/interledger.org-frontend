import { Meta, StoryObj } from '@storybook/react';
import { ParagraphPriceList } from './ParagraphPriceList';

const meta = {
  title: 'Paragraph/ParagraphPriceList',
  component: ParagraphPriceList,
} satisfies Meta<typeof ParagraphPriceList>;

export default meta;
type Story = StoryObj<typeof meta>;

// export const Default: Story = {
//   args: {},
// };
