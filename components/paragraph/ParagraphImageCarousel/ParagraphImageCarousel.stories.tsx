import { Meta, StoryObj } from '@storybook/react';
import { ParagraphImageCarousel } from './ParagraphImageCarousel';

const meta = {
  title: 'Paragraph/ParagraphImageCarousel',
  component: ParagraphImageCarousel,
} satisfies Meta<typeof ParagraphImageCarousel>;

export default meta;
type Story = StoryObj<typeof meta>;

// export const Default: Story = {
//   args: {},
// };
