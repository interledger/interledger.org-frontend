import { Meta, StoryObj } from '@storybook/react';
import { ParagraphScheduleDay } from './ParagraphScheduleDay';

const meta = {
  title: 'Paragraph/ParagraphScheduleDay',
  component: ParagraphScheduleDay,
} satisfies Meta<typeof ParagraphScheduleDay>;

export default meta;
type Story = StoryObj<typeof meta>;

// export const Default: Story = {
//   args: {},
// };
