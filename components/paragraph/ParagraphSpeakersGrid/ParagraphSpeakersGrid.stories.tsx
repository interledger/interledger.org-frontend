import { Meta, StoryObj } from '@storybook/react';
import { ParagraphSpeakersGrid } from './ParagraphSpeakersGrid';

const meta = {
  title: 'Paragraph/ParagraphSpeakersGrid',
  component: ParagraphSpeakersGrid,
} satisfies Meta<typeof ParagraphSpeakersGrid>;

export default meta;
type Story = StoryObj<typeof meta>;

// export const Default: Story = {
//   args: {},
// };
