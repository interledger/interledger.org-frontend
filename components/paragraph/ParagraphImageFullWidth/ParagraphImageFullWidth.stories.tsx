import { Meta, StoryObj } from '@storybook/react';
import { ParagraphImageFullWidth } from './ParagraphImageFullWidth';

const meta = {
  title: 'Paragraph/ParagraphImageFullWidth',
  component: ParagraphImageFullWidth,
} satisfies Meta<typeof ParagraphImageFullWidth>;

export default meta;
type Story = StoryObj<typeof meta>;

// export const Default: Story = {
//   args: {},
// };
