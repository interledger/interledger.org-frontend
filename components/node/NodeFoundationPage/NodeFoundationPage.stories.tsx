import { Meta, StoryObj } from '@storybook/react';
import { NodeFoundationPage } from './NodeFoundationPage';

const meta = {
  title: 'Node/NodeFoundationPage',
  component: NodeFoundationPage,
} satisfies Meta<typeof NodeFoundationPage>;

export default meta;
type Story = StoryObj<typeof meta>;

// export const Default: Story = {
//   args: {},
// };
