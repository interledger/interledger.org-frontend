import { Meta, StoryObj } from '@storybook/react';
import { SlackIcon } from './SlackIcon';

const meta = {
  title: 'Icon/SlackIcon',
  component: SlackIcon,
} satisfies Meta<typeof SlackIcon>;

export default meta;
type Story = StoryObj<typeof meta>;

// export const Default: Story = {
//   args: {},
// };
