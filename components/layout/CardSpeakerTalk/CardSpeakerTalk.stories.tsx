import { Meta, StoryObj } from '@storybook/react';
import { CardSpeakerTalk } from './CardSpeakerTalk';

const meta = {
  title: 'Layout/CardSpeakerTalk',
  component: CardSpeakerTalk,
} satisfies Meta<typeof CardSpeakerTalk>;

export default meta;
type Story = StoryObj<typeof meta>;

// export const Default: Story = {
//   args: {},
// };
